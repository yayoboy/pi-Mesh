fastapi>=0.115
uvicorn>=0.29
aiosqlite>=0.20
meshtastic>=2.5
jinja2>=3.1
python-multipart>=0.0.9
websockets>=13.0
paho-mqtt>=2.0
qrcode>=7.4
# GPIO locale (buzzer, LED, test periferiche). Python puro: parla al
# gpiochip del kernel con delle ioctl, quindi si installa ovunque senza
# compilatore. Su una macchina senza gpiochip l'import non avviene
# proprio, perché la sezione GPIO resta nascosta.
python-periphery>=2.4
