fastapi>=0.115
uvicorn>=0.29
aiosqlite>=0.20
meshtastic>=2.5
jinja2>=3.1
python-multipart>=0.0.9
websockets>=13.0
paho-mqtt>=2.0
qrcode>=7.4
# GPIO locale (buzzer, LED, test periferiche). Wheel precompilate per
# armv7l e aarch64; su una macchina senza gpiochip l'import non avviene
# proprio, perché la sezione GPIO resta nascosta.
lgpio>=0.2
